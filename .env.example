# Vercel Postgres 數據庫連接
# 部署到 Vercel 後會自動配置，本地開發需要手動設置
POSTGRES_URL="postgres://username:password@host:5432/database"
POSTGRES_PRISMA_URL="postgres://username:password@host:5432/database?pgbouncer=true"
POSTGRES_URL_NON_POOLING="postgres://username:password@host:5432/database"
POSTGRES_USER="username"
POSTGRES_HOST="host"
POSTGRES_PASSWORD="password"
POSTGRES_DATABASE="database"

# GPTBots API 配置
# 用於推送用戶屬性到 GPTBots
GPTBOTS_API_KEY="your_gptbots_api_key_here"
GPTBOTS_ENDPOINT="your_endpoint"
# 例如：如果您的 API 地址是 https://api-us.gptbots.ai，則 endpoint 為 "us"

# 注意：
# 1. 本文件是環境變量模板，不要提交真實的密鑰
# 2. 部署到 Vercel 後，在 Vercel 環境變量中設置這些值
# 3. 本地開發時，複製此文件為 .env.local 並填入真實值
